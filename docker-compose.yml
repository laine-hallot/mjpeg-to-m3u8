services:
  stream:
    build: .
    environment:
      - STREAM_URL=${STREAM_URL:?error}
    command: "./bin/mjpeg-to-m3u8.js --out-dir ./out --stream-url $STREAM_URL"
    ports:
      - "8067:8067"
    restart: unless-stopped
